sudo: required
services:
  - docker


before_install:
  - docker build -t ankur05/docker-react -f Dockerfile.dev .

script:
  - docker run ankur05/docker-react npm run test -- --coverage
 
install: travis_wait mvn install